# 🎨 0010capacity 앱 전체 디자인 검토

## 📋 개요

**프로젝트**: 0010capacity 개인 브랜드 플랫폼
**목적**: 소설, 블로그, 앱 마켓플레이스를 제공하는 종합 포트폴리오 서비스
**아키텍처**: Monorepo (Frontend + Backend)
**상태**: 활발한 개발 중

---

## 🏗️ 전체 아키텍처

### 1. 시스템 구조

```
┌─────────────────────────────────────────────────────┐
│           GitHub Pages (Frontend)                    │
│  Next.js 15 + TypeScript + Mantine UI + Zustand    │
│                                                     │
│  [Home] → [Novels] [Blog] [Apps]                    │
│         → [Admin Panel] [Edit Profile]              │
└────────────────┬────────────────────────────────────┘
                 │ (REST API)
┌────────────────▼────────────────────────────────────┐
│        Fly.io (Backend)                              │
│  Rust + Axum + PostgreSQL + SQLx                    │
│                                                     │
│  [Auth] [Novels] [Blog] [Apps] [Profile]            │
└─────────────────────────────────────────────────────┘
```

### 2. 배포 파이프라인

```
GitHub Repository (main branch)
         ↓
┌─────────────────────────────────────┐
│     GitHub Actions CI/CD             │
├─────────────────────────────────────┤
│ ✓ frontend/ 변경 → GitHub Pages    │
│ ✓ backend/ 변경 → Fly.io Deploy    │
└─────────────────────────────────────┘
```

---

## 🎯 기능 아키텍처

### Frontend 페이지 구조

```
/
├── /              (홈페이지 - 3개 카테고리 버튼)
├── /novels        (소설 목록 및 조회)
│   └── /novels/[[...params]]  (동적 라우팅)
├── /blog          (블로그 포스트 목록 및 조회)
│   └── /blog/[[...params]]    (동적 라우팅)
├── /apps          (앱 마켓플레이스)
│   └── /apps/[[...params]]    (동적 라우팅)
├── /about         (개인 소개 페이지)
├── /portfolio     (포트폴리오 페이지)
├── /tech-stack-analysis  (기술 스택 분석)
├── /admin/        (관리자 영역)
│   ├── /admin/login         (로그인 페이지)
│   ├── /admin/dashboard     (관리자 대시보드)
│   ├── /admin/novels        (소설 관리)
│   ├── /admin/blog          (블로그 관리)
│   └── /admin/apps          (앱 관리)
├── /edit-profile  (프로필 편집)
├── /edit-app      (앱 편집)
├── /upload-app    (앱 업로드)
└── /submit-pr     (PR 제출)
```

### Backend API 구조

```
/api/
├── /health              (헬스 체크)
├── /auth/
│   ├── POST /register   (회원가입)
│   ├── POST /login      (로그인)
│   └── POST /logout     (로그아웃)
├── /novels/
│   ├── GET /            (목록 조회)
│   ├── GET /:slug       (상세 조회)
│   ├── POST /           (생성)
│   ├── PUT /:slug       (수정)
│   ├── DELETE /:slug    (삭제)
│   └── /chapters/       (장별 관리)
├── /blog/
│   ├── GET /            (목록 조회)
│   ├── GET /:slug       (상세 조회)
│   ├── POST /           (생성)
│   ├── PUT /:slug       (수정)
│   └── DELETE /:slug    (삭제)
└── /apps/
    ├── GET /            (목록 조회)
    ├── GET /:slug       (상세 조회)
    ├── POST /           (생성)
    ├── PUT /:slug       (수정)
    └── DELETE /:slug    (삭제)
```

---

## 🛠️ 기술 스택 상세 분석

### Frontend Technologies

| 계층 | 기술 | 버전 | 용도 |
|-----|------|------|------|
| **Runtime** | Node.js | - | JavaScript 런타임 |
| **Framework** | Next.js | 15.5.3 | React 메타프레임워크 (App Router) |
| **Language** | TypeScript | - | 타입 안전성 |
| **UI Library** | Mantine Core | 8.3.10 | 컴포넌트 라이브러리 |
| **State Management** | Zustand | 5.0.9 | 가벼운 상태 관리 |
| **Markdown** | react-markdown | 10.1.0 | Markdown 렌더링 |
| **Icons** | Tabler Icons React | 3.0.0 | 아이콘 세트 |
| **Audio** | Tone.js | 15.1.22 | 음성 재생 |
| **Styling** | Mantine CSS | - | 다크 모드 지원 |
| **Linting** | ESLint | 9 | 코드 품질 |
| **Formatting** | Prettier | 3.3.3 | 코드 포맷팅 |
| **Git Hooks** | Husky + lint-staged | 9.0.11 | Pre-commit 검사 |

### Backend Technologies

| 계층 | 기술 | 버전 | 용도 |
|-----|------|------|------|
| **Language** | Rust | 2021 | 안전하고 빠른 언어 |
| **Web Framework** | Axum | 0.7 | 고성능 웹 프레임워크 |
| **Async Runtime** | Tokio | 1 | 비동기 런타임 |
| **Database** | PostgreSQL | 14+ | RDBMS |
| **ORM** | SQLx | 0.7 | 컴파일타임 쿼리 검증 |
| **Serialization** | serde | 1 | JSON 직렬화 |
| **JWT** | jsonwebtoken | 9 | 토큰 기반 인증 |
| **Password Hashing** | Argon2 | 0.5 | 비밀번호 해싱 |
| **UUID** | uuid | 1 | 고유 식별자 |
| **Validation** | validator | 0.18 | 데이터 검증 |
| **Logging** | tracing | 0.1 | 로깅 및 추적 |
| **HTTP** | tower-http | 0.5 | CORS, 트레이싱 미들웨어 |

---

## 🎨 UI/UX 디자인

### 1. 디자인 철학

- **다크 모드 우선**: 기본 테마가 다크 모드로 설정
- **미니멀리즘**: 불필요한 요소 제거
- **한글 타이포그래피**: NanumMyeongjo(명조) 폰트 사용
- **인터랙티브**: 호버 효과 등으로 사용자 피드백 제공
- **반응형**: Mobile부터 Desktop까지 지원

### 2. 컬러 팔레트

```
Primary Color: Neutral (회색)
├── 50: #f5f5f5 (가장 밝음)
├── 100: #e5e5e5
├── 200: #d4d4d4
├── 300: #a3a3a3
├── 400: #737373
├── 500: #525252
├── 600: #404040
├── 700: #262626
├── 800: #171717 (포그라운드)
└── 950: #0a0a0a (배경)
```

### 3. 홈페이지 설계

```
┌─────────────────────────────────────┐
│                                     │
│    (Centered Content Area)          │
│                                     │
│    ┌──────────┐  ┌──────────┐     │
│    │  소설    │  │  블로그  │     │
│    │ 읽으러   │  │ 보러가기 │     │
│    └──────────┘  └──────────┘     │
│         ┌──────────┐               │
│         │   앱     │               │
│         │구경하기  │               │
│         └──────────┘               │
│                                     │
├─────────────────────────────────────┤
│  [이정원에 대해] · [관리]           │
└─────────────────────────────────────┘
```

### 4. 컴포넌트 라이브러리

**핵심 컴포넌트**:
- `Button.tsx`: 커스텀 버튼
- `Card.tsx`: 컨텐츠 카드
- `Input.tsx`: 입력 필드
- `Modal.tsx`: 모달 다이얼로그
- `Form.tsx`: 폼 컨테이너
- `Badge.tsx`: 태그/배지
- `ErrorMessage.tsx`: 에러 표시
- `Loading.tsx`: 로딩 상태

**기능 컴포넌트**:
- `MarkdownEditor.tsx`: Markdown 에디터
- `MusicPlayer.tsx`: 음악 플레이어
- `Snowfall.tsx`: 눈 내리는 애니메이션
- `ThemeToggle.tsx`: 라이트/다크 모드 토글
- `SPARedirectHandler.tsx`: SPA 리다이렉트 처리

---

## 📊 데이터 모델

### Database Schema

```sql
-- 관리자 테이블
admins
├── id (UUID, PK)
├── username (String, UNIQUE)
├── password_hash (String)
└── created_at (DateTime)

-- 소설 테이블
novels
├── id (UUID, PK)
├── slug (String, UNIQUE)
├── title (String)
├── description (String)
├── genre (String)
├── author_id (FK → admins)
├── created_at (DateTime)
└── updated_at (DateTime)

-- 소설 장 테이블
novel_chapters
├── id (UUID, PK)
├── novel_id (FK → novels)
├── chapter_number (Int)
├── title (String)
├── content (Text)
├── created_at (DateTime)
└── updated_at (DateTime)

-- 블로그 포스트 테이블
blog_posts
├── id (UUID, PK)
├── slug (String, UNIQUE)
├── title (String)
├── content (Text)
├── tags (Array<String>)
├── author_id (FK → admins)
├── created_at (DateTime)
└── updated_at (DateTime)

-- 앱 테이블
apps
├── id (UUID, PK)
├── slug (String, UNIQUE)
├── name (String)
├── description (String)
├── repository_url (String)
├── demo_url (String)
├── category (String)
├── author_id (FK → admins)
├── created_at (DateTime)
└── updated_at (DateTime)
```

---

## 🔐 인증 및 권한

### 인증 흐름

```
1. 사용자가 /admin/login에 접속
2. 관리자 계정으로 로그인
3. Backend에서 JWT 토큰 발급
4. 토큰을 localStorage에 저장
5. 이후 API 요청에 Authorization 헤더에 포함
6. Backend에서 토큰 검증 후 요청 처리
```

### JWT 토큰 정보

```javascript
{
  "sub": "admin-user-id",
  "username": "admin",
  "iat": 1234567890,
  "exp": 1234567890 + 604800  // 7일 만료
}
```

### 권한 관리

- **Public**: 모든 사용자 (열람만 가능)
- **Admin**: 관리자만 (생성/수정/삭제 가능)

---

## 🚀 배포 전략

### Frontend 배포 (GitHub Pages)

```yaml
Trigger: frontend/ 디렉토리 변경 + main 브랜치
Process:
  1. npm install
  2. npm run build (next build)
  3. out/ 디렉토리 생성
  4. GitHub Pages에 배포
Result: https://0010capacity.github.io
```

### Backend 배포 (Fly.io)

```yaml
Trigger: backend/ 디렉토리 변경 + main 브랜치
Process:
  1. Rust 컴파일
  2. Docker 이미지 빌드
  3. Fly.io에 배포
  4. PostgreSQL 마이그레이션 실행
Result: https://0010capacity-backend.fly.dev
```

---

## 📱 반응형 디자인

### 브레이크포인트

| 기기 | 너비 | 사용 기술 |
|-----|------|---------|
| **Mobile** | < 768px | Mantine `base` |
| **Tablet** | 768px - 1024px | Mantine 기본 |
| **Desktop** | > 1024px | Mantine `md` |

### 반응형 구현 예

```typescript
<Flex
  direction={{ base: "column", md: "row" }}  // 모바일: 세로, 데스크톱: 가로
  gap={{ base: "md", md: "xl" }}            // 모바일: 중간 간격, 데스크톱: 큰 간격
  align="center"
  justify="center"
>
  {/* Content */}
</Flex>
```

---

## 🔄 상태 관리

### Zustand Store 구조

```typescript
// 예시 스토어 (실제 구현 필요)
type AppStore = {
  // 상태
  user: User | null;
  token: string | null;
  
  // 액션
  setUser: (user: User) => void;
  setToken: (token: string) => void;
  logout: () => void;
};
```

### 전역 상태

1. **인증 상태**: 로그인 여부, 사용자 정보, JWT 토큰
2. **UI 상태**: 현재 테마, 로딩 상태, 모달 상태
3. **음악 플레이어**: 현재 재생 중인 음악, 재생 상태

---

## 🎭 특수 기능

### 1. 음악 플레이어

- **라이브러리**: Tone.js + @tonejs/midi
- **기능**: MIDI 파일 재생
- **구현**: MusicPlayer 컴포넌트, MusicPlayerProvider
- **상태**: MusicPlayerProvider의 Context API 사용

### 2. 눈 내리는 애니메이션

- **기능**: 계절 분위기 제공 (겨울)
- **구현**: Snowfall 컴포넌트
- **상태**: SnowfallProvider의 Context API 사용

### 3. GitHub 연동

- **라이브러리**: @octokit/rest
- **기능**: GitHub 정보 조회, PR 제출 등
- **용도**: 포트폴리오 표시, 개발 활동 연동

### 4. Markdown 에디터

- **라이브러리**: @uiw/react-md-editor
- **기능**: 블로그 및 소설 작성
- **지원**: 문법 강조, 라이브 미리보기

---

## ✅ 개발 표준

### 코드 스타일

```javascript
// ESLint + Prettier 자동 포맷팅
// Pre-commit 훅으로 강제

// 네이밍 컨벤션
Components:    PascalCase  (Button.tsx)
Functions:     camelCase   (getUserData)
Constants:     UPPER_CASE  (API_BASE_URL)
Variables:     camelCase   (userData)
```

### 파일 구조

```
frontend/
├── app/              # Next.js 라우팅
├── components/       # React 컴포넌트
│   └── ui/          # 기본 UI 컴포넌트
├── hooks/           # Custom React Hooks
├── lib/             # 유틸리티 함수
├── public/          # 정적 파일
└── scripts/         # 빌드 스크립트
```

### 타입 안전성

- **모든 Props**: interface 또는 type 정의
- **API 응답**: Zod 또는 TypeScript interface
- **ENV 변수**: type-safe 환경 변수 관리

---

## 🐛 에러 처리

### Frontend

```typescript
// API 요청 에러 처리
try {
  const data = await fetchAPI(endpoint);
} catch (error) {
  // ErrorMessage 컴포넌트로 표시
  showError(error.message);
}
```

### Backend

```rust
// 커스텀 에러 타입
pub enum AppError {
    NotFound,
    Unauthorized,
    BadRequest,
    InternalError,
}

// AppError를 HTTP 응답으로 변환
impl IntoResponse for AppError { ... }
```

---

## 🧪 테스트 전략

### 현재 상태

- Frontend: 테스트 설정 없음 (`npm test` → exit 0)
- Backend: 기본 테스트 인프라 있음 (tokio-test)

### 개선 필요

1. **Frontend**: Vitest 또는 Jest 추가
2. **Backend**: 더 많은 통합 테스트
3. **E2E**: Playwright 또는 Cypress

---

## 📈 성능 최적화

### Frontend

- **Next.js 최적화**:
  - Static Site Generation (SSG)
  - Incremental Static Regeneration (ISR)
  - Image Optimization

- **번들 크기 최적화**:
  - Tree shaking
  - Code splitting (Route-based)
  - Dynamic imports

### Backend

- **데이터베이스**:
  - 커넥션 풀링 (max 5 connections)
  - 인덱싱 (slug 컬럼)

- **캐싱**:
  - HTTP 캐시 헤더 설정
  - 가능하면 CDN 활용

---

## 🔍 모니터링 및 로깅

### Backend 로깅

```rust
// tracing을 통한 구조화된 로깅
tracing::debug!("Event: {}", message);
tracing::info!("Server listening on {}", addr);
tracing::warn!("Deprecated endpoint called");
tracing::error!("Database error: {}", error);
```

### 로그 레벨 설정

```bash
RUST_LOG=debug cargo run
RUST_LOG=backend=trace cargo run
RUST_LOG=backend=debug,tower_http=debug cargo run
```

---

## 🚨 보안 고려사항

### 현재 구현

1. **CORS**: tower-http CorsLayer로 보호
2. **JWT**: 7일 만료 시간
3. **Password**: Argon2 해싱
4. **Database**: SQLx로 SQL injection 방지

### 개선 필요

1. **HTTPS**: 프로덕션에서는 필수
2. **Rate Limiting**: API 요청 제한
3. **CSRF 보호**: 토큰 기반 CSRF 방지
4. **Content Security Policy**: CSP 헤더 설정
5. **API Key 관리**: 환경 변수 보안

---

## 📋 Checklist 및 개선 사항

### ✅ 완료된 항목

- [x] Monorepo 구조 설정
- [x] CI/CD 파이프라인 구성
- [x] Frontend 기본 레이아웃
- [x] Backend API 기본 구조
- [x] 데이터베이스 스키마
- [x] 인증 시스템
- [x] 다크 모드 UI

### 🔄 진행 중

- [ ] 모든 API 엔드포인트 구현
- [ ] 관리자 대시보드 완성
- [ ] 이미지 업로드 기능
- [ ] 검색 기능

### ⏳ 계획 중

- [ ] 전체 테스트 커버리지
- [ ] 성능 최적화
- [ ] 세부 보안 강화
- [ ] 모바일 앱 버전 (Flutter/React Native)

---

## 📞 관련 문서

- `README.md`: 프로젝트 개요 및 시작 가이드
- `SETUP.md`: 로컬 개발 환경 설정
- `.github/copilot-instructions.md`: 개발 가이드라인

---

## 🎯 핵심 가치

> **0010capacity**는 개발자의 포트폴리오, 창작물, 그리고 기술 공유를 한 플랫폼에서 제공하는 **종합 개인 브랜드 플랫폼**입니다.

**기술적 특징**:
- 🏗️ 모던한 Monorepo 구조
- ⚡ 고성능 Rust 백엔드
- 🎨 사용자 중심의 UI/UX
- 🔐 보안을 고려한 인증 시스템
- 🚀 자동화된 배포 파이프라인

---

**작성일**: 2024년
**버전**: v0.1.0
**상태**: 활발한 개발 중